apiVersion: v1
kind: Service
metadata:
  name: saleor-dashboard
  namespace: frontend
spec:
  type: ClusterIP
  selector:
    app: saleor-dashboard
  ports:
    - port: 80        # Service가 노출할 포트
      targetPort: 80  # Pod 컨테이너 포트도 80으로 맞춤
      protocol: TCP
